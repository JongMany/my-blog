/**
 * 비즈니스 로직 관련 상수들
 */

/**
 * 프로젝트 카드에서 표시할 최대 태그 개수
 */
export const MAX_VISIBLE_TAGS = 3;

/**
 * 프로젝트 커버 이미지 최대 너비 (픽셀)
 */
export const MAX_COVER_IMAGE_WIDTH_PX = 600;

/**
 * Mermaid 다이어그램 렌더링 지연 시간 (밀리초)
 */
export const MERMAID_RENDER_DELAY_MS = 100;

/**
 * Mermaid 다이어그램에서 루트 노드의 인덱스
 */
export const MERMAID_ROOT_NODE_INDEX = 0;

